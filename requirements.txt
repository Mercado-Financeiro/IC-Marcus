pandas>=2.1
pyarrow>=14.0
plotly>=5.20
altair>=5.2
vega_datasets>=0.9
ipykernel>=6.29
nbformat>=5.10
numpy>=1.26

# =============================================================================
# CRYPTO DATA COLLECTOR - DEPENDÊNCIAS
# =============================================================================

# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
alembic>=1.11.0

# Configuration Management
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP Clients & APIs
requests>=2.31.0
websocket-client>=1.6.0
aiohttp>=3.8.0

# Database Drivers
# SQLite (built-in)
psycopg2-binary>=2.9.0  # PostgreSQL driver

# Web Framework & API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0  # FastAPI templates

# Monitoring & Metrics
prometheus-client>=0.17.0
json-logging>=1.3.0  # Structured logging
psutil>=5.9.0        # System monitoring

# Scheduling & Task Management
APScheduler>=3.10.0
schedule>=1.2.0

# Data Processing & Analysis
matplotlib>=3.7.0  # For analysis examples
seaborn>=0.12.0     # For visualization examples
pyarrow>=14.0.0     # For parquet files

# Development & Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0       # Code formatting
isort>=5.12.0       # Import sorting
flake8>=6.0.0       # Linting
mypy>=1.5.0         # Type checking

# Optional: Production Enhancements
gunicorn>=21.2.0    # WSGI server for production
redis>=4.6.0        # Caching (optional)
celery>=5.3.0       # Task queue (optional)

# Optional: Alerting & Notifications
slack-sdk>=3.21.0   # Slack notifications
discord-webhook>=1.3.0  # Discord notifications

# Optional: Security
cryptography>=41.0.0  # SSL/TLS support
bcrypt>=4.0.0         # Password hashing

# Optional: Performance
cython>=3.0.0       # Potential performance improvements
numba>=0.57.0       # JIT compilation for numerical operations

# Análise de Séries Temporais
arch>=6.3.0         # Para modelos GARCH
ta>=0.10.0          # TA-Lib para análise técnica
TA-Lib>=0.4.25       # TA-Lib library for technical analysis
pywavelets>=1.4.1   # Transformadas wavelet
tensorflow>=2.17.0  # Framework de machine learning
keras-tuner>=1.4.0  # Para tuning de hiperparâmetros em Keras/TF
xgboost>=2.0.0      # XGBoost para gradient boosting
optuna>=3.0.0       # Otimização bayesiana de hiperparâmetros
scipy>=1.9.0        # Análises estatísticas (QQ-plot, distribuições)
shap>=0.42.0        # Explicabilidade dos modelos
statsmodels>=0.14.0 # Modelagem estatística avançada
polars>=0.20.0      # High-performance DataFrame library
scikit-learn>=1.4.0 # Escalonamento e métricas clássicas

# =============================================================================
# PRODUCTION RECOMMENDATIONS
# =============================================================================
#
# For production deployments, consider:
#
# 1. Pin exact versions instead of using >=
# 2. Use requirements-lock.txt with pip-tools
# 3. Add production-specific dependencies:
#    - nginx (reverse proxy)
#    - supervisor (process management)
#    - prometheus + grafana (monitoring stack)
#    - elk stack (logging)
#
# 4. Security scanning:
#    pip install safety bandit
#    safety check
#    bandit -r src/
#
# =============================================================================
pyarrow
